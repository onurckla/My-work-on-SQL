--sort the different values in the replacement_cost column in the movie table.
SELECT DISTINCT (replacement_cost) FROM film;
--how many different data are there in the replacement_cost column in the movie table?
SELECT COUNT (DISTINCT replacement_cost) FROM film;
--how many of the movie names (titles) in the movie table start with the T character and at the same time the rating is equal to 'G'?
SELECT COUNT (title) FROM film WHERE title LIKE 'T%' AND rating = 'G';
--how many of the country names in the country table (country) consist of 5 characters?
SELECT COUNT(country) FROM country WHERE country ILIKE '_____'
--how many of the city names in the city table end with the character 'R' or r?
SELECT COUNT(city) FROM city WHERE city ILIKE '%r'